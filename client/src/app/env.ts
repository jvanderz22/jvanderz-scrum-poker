// Single place to point the client at the backend. Override at build time
// however you like (e.g. replace this file, or read from window at runtime)
// if you deploy client and server on different hosts.
export const API_BASE = 'http://localhost:3000';
